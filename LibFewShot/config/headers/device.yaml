# finished ****************************************************

#device_ids: 0 # determine GPU ids in bus order
#n_gpu: 1 # select the number of gpus to use
#seed: 2147483647 # random seed for numpy, torch and cuda
#deterministic: True # option for torch.backends.cudnn.benchmark  and torch.backends.cudnn.deterministic
#port: ~

device_ids: 0,1 # determine GPU ids in bus order  未定
n_gpu: 1 # select the number of gpus to use  未定
seed: 1 # random seed for numpy, torch and cuda
deterministic: True # option for torch.backends.cudnn.benchmark  and torch.backends.cudnn.deterministic
port: ~